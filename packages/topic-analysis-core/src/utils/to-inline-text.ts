/**
 * 改行を含むテキストを1行に畳む。
 *
 * トピックの説明は3文以内の複数行になり得るため、トピックを
 * 「1行1レコード」で列挙する Phase2(統合)/Phase3(割当) のプロンプトに
 * そのまま埋め込むと、2文目以降が行頭の番号/IDを持たない独立行に見えてしまう。
 * 連続する空白（改行含む）を単一スペースに正規化し、行ベースの列挙を崩さない。
 */
export function toInlineText(text: string): string {
  return text.replace(/\s+/g, " ").trim();
}
